# Gotify environment configuration
# Copy to app.env and customize before starting

# Timezone
TZ=America/New_York

# Admin credentials (used on first run to create the default user)
APP_ADMIN_USER=admin
APP_ADMIN_PASS=changeme_admin_password
